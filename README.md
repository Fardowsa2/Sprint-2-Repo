# Sprint-2-Repo

This project implements a basic version of the SOS game using Java Swing with a graphical user interface (GUI). The current implementation focuses on the initial game setup, user interaction for S/O placement, and basic game options.
